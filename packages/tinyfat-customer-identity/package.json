{
	"name": "@tinyfat/customer-identity",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"description": "Encrypted customer channels, contacts, endpoints, provider threads, and verified links",
	"exports": "./src/index.mjs",
	"scripts": {
		"test": "node --test src/*.test.mjs"
	},
	"engines": {
		"node": ">=22.0.0"
	}
}
